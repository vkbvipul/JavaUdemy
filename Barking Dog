public class BarkingDog {
 public static boolean shouldWakeUp(boolean barking, int hourOfDay)
 {
     if(barking == true && hourOfDay < 24 && hourOfDay > -1)
     {
        if(hourOfDay < 8 || hourOfDay > 22)
        {
         return true;
        }
      
     }
     return false;
 }
}
